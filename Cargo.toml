[package]
name = "vicarian"
description = "Vicarian is a TLS-first reverse-proxy server with ACME support"
version = "0.3.4"
edition = "2024"

authors = ["Steve Smith <tarkasteve@gmail.com>"]
homepage = "https://github.com/tarka/vicarian"
repository = "https://github.com/tarka/vicarian"
readme = "README.md"

keywords = ["proxy", "https", "reverse-proxy", "pingora"]
categories = ["web-programming::http-server", "network-programming"]
license = "Apache-2.0"

[dependencies]
anyhow = "1.0.104"
async-trait = "0.1.92"
bytes = "1.12.1"
camino = { version = "1.2.5", features = ["serde1"] }
clap = { version = "4.6.6", features = ["derive"] }
dnsclient = "0.2.0"
fastrand = "2.5.0"
futures = "0.3.34"
futures-lite = "2.6.1"
hcl-rs = "0.19.8"
http = "1.5.0"
http-body-util = "0.1.5"
http-serde = "2.1.1"
hyper = "1.11.1"
instant-acme = { version = "0.8.5", features = ["time"] }
itertools = "0.15.0"
metrics = "0.24.6"
metrics-exporter-prometheus = { version = "0.18.3", default-features = false, features = ["tokio", "tracing"] }
nix = { version = "0.31.3", features = ["net", "resource"] }
notify = { version = "8.2.0", features = ["crossbeam-channel"] }
notify-debouncer-full = { version = "0.7.0", features = ["crossbeam-channel"] }
papaya = "0.2.5"
phf = { version = "0.14.0", features = ["phf_macros"] }
phf_macros = "0.14.0"
pingora-boringssl = "0.8.1"
pingora-core = { version = "0.8.1", features = ["boringssl"] }
pingora-http = "0.8.1"
pingora-proxy = "0.8.1"
psl = "2.1.231"
rustls = "0.23.44"
serde = { version = "1.0.229", features = ["derive"] }
serde_default_utils = { version = "0.3.1", features = ["inline"] }
serde_json = "1.0.151"
static-web-server = { version = "3.0.0-beta.1", default-features = false, features = ["compression", "directory-listing", "fallback-page"] }
strum_macros = "0.28.0"
thiserror = "2.0.20"
time = { version = "0.3.55", features = ["local-offset"] }
tokio = { version = "1.53.1", features = ["macros", "rt", "sync", "fs"] }
tracing = "0.1.44"
tracing-log = "0.2.0"
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }
x509-parser = "0.18.1"
zone-update = { version = "0.12.2", features = ["async"] }

[features]
default = ["integration_tests"]
integration_tests = []

[dev-dependencies]
fslock = "0.2.1"
nix = { version = "0.31.3", features = ["process", "signal", "term"] }
pem = "4.0.0"
rcgen = { version = "0.14.10", features = ["x509-parser"] }
reqwest = { version = "0.13.4", default-features = false, features = ["http2", "rustls"] }
serial_test = "4.0.1"
tempfile = "3.27.0"
test-log = { version = "0.2.21", features = ["trace"] }
time = { version = "0.3.55", features = ["macros"] }
tokio = { version = "1.53.1", features = ["fs"] }
tokio-tungstenite = { version = "0.30.0", features = ["rustls", "rustls-tls-webpki-roots", "tokio-rustls"] }
wiremock = "0.6.5"
wiremocket = "0.3.2"

[profile.release]
strip = true
overflow-checks = true
lto = "fat"
codegen-units = 1

[lints.clippy]
unused_unit = "allow"

[package.metadata.deb]
extended-description = """\
Vicarian is a TLS-first reverse proxy server with built-in
ACME/LetsEncrypt support (HTTP-01 and DNS-01). It also
supports native static file serving."""
section = "Network"
maintainer-scripts = "systemd"
assets = [
    ["target/release/vicarian", "usr/bin/", "755"],
    ["examples/vicarian-full.hcl", "etc/vicarian/vicarian.hcl", "644"],
    ["CHANGELOG.md", "usr/share/doc/vicarian/", "644"],
    ["METRICS.md", "usr/share/doc/vicarian/", "644"],
    ["LICENSE", "usr/share/doc/vicarian/", "644"],
    ["README.md", "usr/share/doc/vicarian/", "644"],
    ["SECURITY.md", "usr/share/doc/vicarian/", "644"],
    ["examples/*", "usr/share/doc/vicarian/examples/", "644"],
    ["systemd/secrets", "etc/vicarian/", "640"],
]

[package.metadata.deb.systemd-units]
unit-scripts = "systemd"
enable = true
start = true

[package.metadata.binstall]
bin-dir = "{ name }-v{ version }-{ target }/bin/{ bin }{ binary-ext }"

[package.metadata.bacon]
apply_gitignore = false
watch = ["vicarian.hcl"]
