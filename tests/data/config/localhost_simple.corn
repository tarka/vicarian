{
    listen = {
        insecure_port = 18080
        tls_port = 18443
    }
    vhosts = [
        {
            hostname = "localhost"

            tls = {
                files = {
                    keyfile = "target/certs/localhost.key"
                    certfile = "target/certs/localhost.crt"
                    reload = true
                }
            }

            backends = [
                {
                    context = "/"
                    url = "http://127.0.0.1:19090"
                }
            ]
        }
    ]
}
